# Optional Docker/runtime overrides. The zero-configuration Docker path does
# not require this file; copy it to .env only when changing a default.
# ARTIFACTS_PORT=4242
# ARTIFACTS_PUBLIC_BASE_URL=http://localhost:4242
# ARTIFACTS_FILES_MOUNT=artifacts-files
# ARTIFACTS_DATABASE_MOUNT=artifacts-database
